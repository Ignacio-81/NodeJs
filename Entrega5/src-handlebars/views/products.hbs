<h2>Add Products:</h2>

<a href="http://localhost:3000/static/index.html">
  <button class="">Load your Product</button>
</a>

{{!-- <h1>My Products Shop</h1>
{{#if hasAny}}
  {{#each products}}

    <h2>{{this.title}}</h2>
    <img src="{{this.thumbnail}}" alt="" width="100" />
    <p>{{this.price}}</p>
  {{/each}}
{{else}}
  <h2>No product Found</h2>
{{/if}} --}}

<table class="table align-middle mb-0 bg-white">
  <thead class="bg-light">
    <tr>
      <th>Thumbnail</th>
      <th>Title</th>
      <th>Price</th>
    </tr>
  </thead>
  <tbody>
    {{#if hasAny}}
      {{#each products}}
        <tr>
          <td>
            <div class="d-flex align-items-center">
              <img
                src="{{this.thumbnail}}"
                alt=""
                style="width: 100px; height: 100px"
                {{! class="rounded-circle" }}
              />
              {{!           <div class="ms-3">
            <p class="fw-bold mb-1">John Doe</p>
            <p class="text-muted mb-0">john.doe@gmail.com</p>
          </div> }}
            </div>
          </td>
          <td>
            <p class="fw-normal mb-1">{{this.title}}</p>
          </td>
          <td>
            <p class="rounded-pill d-inline">
              $
              {{this.price}}
            </p>
          </td>
          {{!       <td>
        <button type="button" class="btn btn-link btn-sm btn-rounded">
          Edit
        </button>
      </td> }}
        </tr>
      {{/each}}
    {{else}}
      <tr>
        <td>
          <h2>No product Found</h2>
        </td>
      </tr>
    {{/if}}
  </tbody>
</table>